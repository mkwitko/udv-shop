/**
* Generated by Kubb (https://kubb.dev/).
* Do not edit manually.
*/

import fetch from "../../fetch-client.ts";
import type { Client, RequestConfig, ResponseErrorConfig } from "../../fetch-client.ts";
import type { ArchiveProductMutationResponse, ArchiveProductPathParams } from "../types/ArchiveProduct.ts";

function getArchiveProductUrl(slug: ArchiveProductPathParams["slug"], productSlug: ArchiveProductPathParams["productSlug"]) {
  const res = { method: 'DELETE', url: `/stores/${slug}/products/${productSlug}` as const }
  return res
}

/**
 * {@link /stores/:slug/products/:productSlug}
 */
export async function archiveProduct(slug: ArchiveProductPathParams["slug"], productSlug: ArchiveProductPathParams["productSlug"], config: Partial<RequestConfig> & { client?: Client } = {}) {
  const { client: request = fetch, ...requestConfig } = config



  const res = await request<ArchiveProductMutationResponse, ResponseErrorConfig<Error>, unknown>({ method : "DELETE", url : getArchiveProductUrl(slug, productSlug).url.toString(), ... requestConfig })
  return res.data
}