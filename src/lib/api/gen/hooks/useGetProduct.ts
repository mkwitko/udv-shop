/**
* Generated by Kubb (https://kubb.dev/).
* Do not edit manually.
*/

import type { Client, RequestConfig, ResponseErrorConfig } from "../../fetch-client.ts";
import type { GetProductQueryResponse, GetProductPathParams } from "../types/GetProduct.ts";
import type { QueryKey, QueryClient, QueryObserverOptions, UseQueryResult } from "@tanstack/react-query";
import { getProduct } from "../clients/getProduct.ts";
import { queryOptions, useQuery } from "@tanstack/react-query";

export const getProductQueryKey = (slug: GetProductPathParams["slug"] | undefined, productSlug: GetProductPathParams["productSlug"] | undefined) => [{ url: '/stores/:slug/products/:productSlug', params: {slug:slug,productSlug:productSlug} }] as const

export type GetProductQueryKey = ReturnType<typeof getProductQueryKey>

export function getProductQueryOptions(slug: GetProductPathParams["slug"] | undefined, productSlug: GetProductPathParams["productSlug"] | undefined, config: Partial<RequestConfig> & { client?: Client } = {}) {

        const queryKey = getProductQueryKey(slug, productSlug)
        return queryOptions<GetProductQueryResponse, ResponseErrorConfig<Error>, GetProductQueryResponse, typeof queryKey>({
         enabled: !!(slug && productSlug),
         queryKey,
         queryFn: async ({ signal }) => {
            return getProduct(slug!, productSlug!, { ...config, signal: config.signal ?? signal })
         },
        })

}

/**
 * {@link /stores/:slug/products/:productSlug}
 */
export function useGetProduct<TData = GetProductQueryResponse, TQueryData = GetProductQueryResponse, TQueryKey extends QueryKey = GetProductQueryKey>(slug: GetProductPathParams["slug"] | undefined, productSlug: GetProductPathParams["productSlug"] | undefined, options: 
{
  query?: Partial<QueryObserverOptions<GetProductQueryResponse, ResponseErrorConfig<Error>, TData, TQueryData, TQueryKey>> & { client?: QueryClient },
  client?: Partial<RequestConfig> & { client?: Client }
}
 = {}) {

         const { query: queryConfig = {}, client: config = {} } = options ?? {}
         const { client: queryClient, ...resolvedOptions } = queryConfig
         const queryKey = resolvedOptions?.queryKey ?? getProductQueryKey(slug, productSlug)
         

         const query = useQuery({
          ...getProductQueryOptions(slug, productSlug, config),
          ...resolvedOptions,
          queryKey,
         } as unknown as QueryObserverOptions, queryClient) as UseQueryResult<TData, ResponseErrorConfig<Error>> & { queryKey: TQueryKey }

         query.queryKey = queryKey as TQueryKey

         return query
         
}