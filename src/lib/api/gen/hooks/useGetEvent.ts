/**
* Generated by Kubb (https://kubb.dev/).
* Do not edit manually.
*/

import type { Client, RequestConfig, ResponseErrorConfig } from "../../fetch-client.ts";
import type { GetEventQueryResponse, GetEventPathParams } from "../types/GetEvent.ts";
import type { QueryKey, QueryClient, QueryObserverOptions, UseQueryResult } from "@tanstack/react-query";
import { getEvent } from "../clients/getEvent.ts";
import { queryOptions, useQuery } from "@tanstack/react-query";

export const getEventQueryKey = (slug: GetEventPathParams["slug"] | undefined, eventSlug: GetEventPathParams["eventSlug"] | undefined) => [{ url: '/stores/:slug/events/:eventSlug', params: {slug:slug,eventSlug:eventSlug} }] as const

export type GetEventQueryKey = ReturnType<typeof getEventQueryKey>

export function getEventQueryOptions(slug: GetEventPathParams["slug"] | undefined, eventSlug: GetEventPathParams["eventSlug"] | undefined, config: Partial<RequestConfig> & { client?: Client } = {}) {

        const queryKey = getEventQueryKey(slug, eventSlug)
        return queryOptions<GetEventQueryResponse, ResponseErrorConfig<Error>, GetEventQueryResponse, typeof queryKey>({
         enabled: !!(slug && eventSlug),
         queryKey,
         queryFn: async ({ signal }) => {
            return getEvent(slug!, eventSlug!, { ...config, signal: config.signal ?? signal })
         },
        })

}

/**
 * {@link /stores/:slug/events/:eventSlug}
 */
export function useGetEvent<TData = GetEventQueryResponse, TQueryData = GetEventQueryResponse, TQueryKey extends QueryKey = GetEventQueryKey>(slug: GetEventPathParams["slug"] | undefined, eventSlug: GetEventPathParams["eventSlug"] | undefined, options: 
{
  query?: Partial<QueryObserverOptions<GetEventQueryResponse, ResponseErrorConfig<Error>, TData, TQueryData, TQueryKey>> & { client?: QueryClient },
  client?: Partial<RequestConfig> & { client?: Client }
}
 = {}) {

         const { query: queryConfig = {}, client: config = {} } = options ?? {}
         const { client: queryClient, ...resolvedOptions } = queryConfig
         const queryKey = resolvedOptions?.queryKey ?? getEventQueryKey(slug, eventSlug)
         

         const query = useQuery({
          ...getEventQueryOptions(slug, eventSlug, config),
          ...resolvedOptions,
          queryKey,
         } as unknown as QueryObserverOptions, queryClient) as UseQueryResult<TData, ResponseErrorConfig<Error>> & { queryKey: TQueryKey }

         query.queryKey = queryKey as TQueryKey

         return query
         
}