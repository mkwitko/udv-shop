/**
* Generated by Kubb (https://kubb.dev/).
* Do not edit manually.
*/

import type { Client, RequestConfig, ResponseErrorConfig } from "../../fetch-client.ts";
import type { GetStoreQueryResponse, GetStorePathParams } from "../types/GetStore.ts";
import type { QueryKey, QueryClient, QueryObserverOptions, UseQueryResult } from "@tanstack/react-query";
import { getStore } from "../clients/getStore.ts";
import { queryOptions, useQuery } from "@tanstack/react-query";

export const getStoreQueryKey = (slug: GetStorePathParams["slug"] | undefined) => [{ url: '/stores/:slug', params: {slug:slug} }] as const

export type GetStoreQueryKey = ReturnType<typeof getStoreQueryKey>

export function getStoreQueryOptions(slug: GetStorePathParams["slug"] | undefined, config: Partial<RequestConfig> & { client?: Client } = {}) {

        const queryKey = getStoreQueryKey(slug)
        return queryOptions<GetStoreQueryResponse, ResponseErrorConfig<Error>, GetStoreQueryResponse, typeof queryKey>({
         enabled: !!(slug),
         queryKey,
         queryFn: async ({ signal }) => {
            return getStore(slug!, { ...config, signal: config.signal ?? signal })
         },
        })

}

/**
 * {@link /stores/:slug}
 */
export function useGetStore<TData = GetStoreQueryResponse, TQueryData = GetStoreQueryResponse, TQueryKey extends QueryKey = GetStoreQueryKey>(slug: GetStorePathParams["slug"] | undefined, options: 
{
  query?: Partial<QueryObserverOptions<GetStoreQueryResponse, ResponseErrorConfig<Error>, TData, TQueryData, TQueryKey>> & { client?: QueryClient },
  client?: Partial<RequestConfig> & { client?: Client }
}
 = {}) {

         const { query: queryConfig = {}, client: config = {} } = options ?? {}
         const { client: queryClient, ...resolvedOptions } = queryConfig
         const queryKey = resolvedOptions?.queryKey ?? getStoreQueryKey(slug)
         

         const query = useQuery({
          ...getStoreQueryOptions(slug, config),
          ...resolvedOptions,
          queryKey,
         } as unknown as QueryObserverOptions, queryClient) as UseQueryResult<TData, ResponseErrorConfig<Error>> & { queryKey: TQueryKey }

         query.queryKey = queryKey as TQueryKey

         return query
         
}