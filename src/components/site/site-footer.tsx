export function SiteFooter() {
  return (
    <footer className="mt-24 border-t border-[var(--line)] py-10">
      <div className="shell flex flex-col gap-3 text-sm text-ink-soft sm:flex-row sm:items-baseline sm:justify-between">
        <p className="font-display text-base text-ink">Lojinha dos Núcleos</p>
        <p>Cada loja é cuidada pelo seu próprio núcleo. Os valores ficam com quem faz.</p>
      </div>
    </footer>
  );
}
